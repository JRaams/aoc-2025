# Solution runtime plotting

## 1. Getting data for a solution

```bash
$ cd src/01

$ bench
```

Then update `gnuplot/runtimes.csv` with the `Time (mean)` value

## 2. Generating the plot as png image

```bash
$ plot
```
